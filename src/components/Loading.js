import { LoaderContainer, LoaderWrapper } from "../styles/loading";

const Loading = () => {
    return (
        <LoaderWrapper>
            <LoaderContainer />
        </LoaderWrapper>
    )
};

export default Loading;